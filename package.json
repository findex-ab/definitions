{
  "name": "findef",
  "version": "2.2.9",
  "description": "shared types",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "typesVersions": {
    "findef": {
      "index": [
        "lib/index.d.ts"
      ]
    }
  },
  "scripts": {
   "build:exports": "npx tsx ./scripts/bundle.ts ./src",
   "build:lib": "npm run build:exports && tsc -p ./tsconfig.lib.json && npm link",
   "build": "tsc && cp -r ./lib/* ./dist/."
  },
  "files": [
    "/lib"
  ],
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/bson": "^4.2.0",
    "bson": "^6.2.0",
    "superstruct": "^1.0.3"
  }
}
